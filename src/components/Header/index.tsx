import React from 'react';
import { Button } from '@mui/material';

const Header = () => {
  return (
    <Button variant="contained">
      <h1>Hello, World!</h1>
    </Button>
  );
};

export default Header;
